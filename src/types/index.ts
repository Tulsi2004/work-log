import type {
  Note,
  NoteCategory,
  AttendanceStatus,
  CompanyStatus,
  ProjectStatus,
  WorkLogStatus,
} from "@prisma/client";

export type { Note, NoteCategory, AttendanceStatus, CompanyStatus, ProjectStatus, WorkLogStatus };
